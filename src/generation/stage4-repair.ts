import type { Env } from "../index";

// Placeholder: later parse tsc/eslint output and do targeted fixes.
export async function repairIfNeeded(artifacts: Record<string, string>, _env: Env) {
  return artifacts; // no-op for now
}
